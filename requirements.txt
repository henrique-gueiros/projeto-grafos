# Dependências do projeto — Python 3.11+
# Algoritmos (BFS, DFS, Dijkstra, Bellman-Ford): implementação própria, sem libs externas.

# Visualizações estáticas (Requisitos 7 e 8)
matplotlib>=3.7
numpy>=1.24

# Visualizações existentes (analise_visual.py — layout gráfico apenas)
networkx>=3.1

# Frontend React — servidor de API
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
